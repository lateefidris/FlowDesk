<p style="color: green"><%= notice %></p>

<h1>Service Categories</h1>

<div id="categories">
  <% current_user.desks.categories.each do |category| %>
    <ul>
    <li>
      <%= link_to category.name, category_path(category) %></li>
    </ul>
  <% end %>
</div>

<%= link_to "New category", new_category_path %>
